# 数据模型

[English](data-model.md) | **简体中文**

- [Run / Agent / Turn](#run--agent--turn)
- [准确性说明](#准确性说明)
- [数据库](#数据库)
- [成本估算](#成本估算)
- [套餐权重](#套餐权重)
- [上下文占用](#上下文占用)

## Run / Agent / Turn

记录到的活动按三个嵌套层级组织：

- **Run（运行）** —— 一次逻辑执行（一次 AI 会话）。可以包含一个或多个智能体。
  没有子智能体的普通对话恰好包含一个智能体；派生了 Task 子智能体的编排者则包含父级
  加上所有后代，它们通过 provider 原生的父/子标记相互关联。
- **Agent（智能体）** —— 一个对话上下文。对应单个转录文件（Claude Code 中即一个
  `.jsonl`）。拥有模型、工作目录、标题和一组回合。
- **Turn（回合）** —— 一次 **API 调用**（一次 LLM 请求/响应）。

子智能体是从 provider 自身的数据中识别出来的 —— 对 Claude Code 而言就是
`<parent>/subagents/agent-*.jsonl` 这一目录约定。不用启发式规则，不做内容嗅探。
如果某个框架没有在转录文件之间记录父子关系，那么每个转录文件就是一个单智能体运行；
这是正确行为，不是缺陷。

## 准确性说明

**Token 去重。** Claude Code 会为响应的每个 *content block* 写一行 JSONL，而 fork 出
的会话会把父会话的历史原样重放进一个新的转录文件。因此回合按 `request_id ??
message_id` 做**全局**去重 —— 与 Claude Code 自己的用量扫描所用的键和优先级完全一致。
不做 content block 去重，输出 token 会被高估约 2.4 倍；不做全局去重，fork 之前的每次
调用都会被计两次，而子代理 fork 从 Claude Code 2.1.232 起默认开启。

**Fork 记录。** 被重放的记录保留原始的 `message.id`、`requestId` 和 `usage`，并带上
`forkedFrom`。这类记录会被直接跳过 —— 回合和事件都是 —— 于是真正花掉这些 token 的
会话保留它们。fork 自身的新活动不带 `forkedFrom`，正常计入。

**部分用量。** 同一响应的多行并不总是一致：流式响应可能在第一行记录部分
`output_tokens`，到最后一行才是完整值，末尾还可能出现一条全零的 usage 记录。因此
token 列取一个响应各行中的**最大值** —— 部分值必然是最终值的前缀。Claude Code 自己
的本地扫描取的是第一行，所以它 `/usage` 活动面板的输出数偏低 —— 实测某一周内，
1,569 次响应中有 36 次、合计约 27k token。

**失败的调用。** 以模型 `<synthetic>` 记录的错误回显不计入统计，也不进入模型列表；
它们在会话树中显示为错误节点。

**日期边界。** "今日"和按天分桶使用你所在时区的自然日，而非 UTC。

**标题。** 运行标题优先取自转录文件的 `ai-title` 记录（Claude Code 自己生成的 AI 会话
标题）；没有时则取第一条真实用户提示词，并剥离 IDE/框架的包装标签。

**缓存写入 TTL。** 5m/1h 的拆分来自 `usage.cache_creation`。仅带总量的历史记录会被
归入 5m 桶（默认 TTL），以免高估成本。

**归因。** Claude Code 会在该组件活跃期间的每一次 API 调用上打上 `attributionSkill`、
`attributionAgent`、`attributionPlugin` 和 `attributionMcpServer`。这些字段决定调用的
成本桶，并驱动按 skill、按服务器的合计，因此"这个 skill 花了多少"指的是它运行的整段
跨度，而不是触发它的那一次调用。这些字段出现之前写下的记录，回退到按调用自身的
`tool_use` 块分类。

**记录值 vs 估算值。** 触发次数、工具调用和 skill 调用来自解析后的事件流 —— 是实际
记录的，不是推断的。UI 中任何标注 *est.* 的数字（注入提示词成本、工具载荷大小）都是
分词器估算值。

## 数据库

SQLite，位于 `data/cache.db`，WAL 模式。共六张表，每张都带一个 `provider` 列以支持
多数据源。

| 表 | 一行代表 | 说明 |
|---|---|---|
| **`files`** | 一个转录文件 | 路径 + 字节偏移，用于增量解析 |
| **`runs`** | 一次逻辑运行 | 派生的汇总：标题、工作目录、智能体数、回合数、首次/最后出现时间，以及 `run_key`（见下）。每次全量扫描**以及**每次增量摄入（防抖）后都会重建，因此 Runs 页面无需重启即可保持实时 |
| **`agents`** | 一个转录文件 | `run_id`、`parent_agent_id`、`parent_turn_index`（同级排序）、`agent_type`、`description`（来自子智能体的 `meta.json`）、标题、工作目录、最后出现时间、回合数 |
| **`turns`** | 一次 API 调用 | **`dedupe_key`（`request_id ?? message_id`）全局唯一** —— 正是这个索引同时消除了"每个 content block 一行"格式和 fork 重放历史带来的重复。携带模型、token 计数、时间戳、四个 `attribution_*` 列，以及 `bucket` 列（0 = 基础，1 = MCP，2 = skill，取自归因，回退到该次调用的 `tool_use` 块；子智能体归属用 `is_subagent`） |
| **`events`** | 一个已解析事件 | 按 (agent_id, 源 uuid) 幂等：用户提示词、工具调用（含工具名与 `tool_use_id`）、hook 触发、API 错误、上下文压缩、模型回退。工具事件还携带估算的 token 体积，Skill 调用另带 skill 名称 |
| **`harness_snapshots`** | 一份 harness 指纹 | 只追加，且仅在指纹发生变化时写入。记录指令文件、技能、命令、hook、MCP 服务器、权限与设置层的组件哈希与 token 数 —— 绝不记录文件内容 |

`turns` 是 token 总量、回合数和最后出现时间的事实来源 —— 这些值全部由它重新计算，
绝不信任任何手工维护的计数器。（否则，增量解析 `ai-title`、`mode` 或 `summary` 这类
末尾无时间戳的记录时，会把回合数清零或把 `last_seen_at` 置空。）

**Schema 版本。** schema 版本存放在 `PRAGMA user_version` 中。应用启动时若发现版本不
一致，就会丢弃并重建所有表，然后重新解析全部转录文件。删除 `data/cache.db` 会触发
同样的重建。任何时候重建都是安全的：JSONL 转录文件才是唯一的事实来源。

## 运行 ID（run key）

`runs.run_key` 是用户提供给对比工具的短公开 ID（`r-9f3a1c2b7e04`），取
`sha256(JSON.stringify([provider, run_id]))` 的前 48 位 —— **推导得到，而非分配**，
因为上面那种重建会让自增 ID 或随机 UUID 变成另一个值，从而悄悄指向另一个会话。把
provider 纳入哈希可保证跨数据源唯一，因为会话 ID 只在单个工具内唯一。

它在 `refreshRuns` 中计算，而每个 provider 本来就会调用该函数，因此新适配器无需额外
代码即可获得 run key。索引刻意**不设唯一约束**：哈希冲突应当在查询时表现为「ID 有歧义」，
而不是变成打断摄入的约束冲突。

## Harness 快照

在其他所有功能里，harness 配置都是从磁盘实时读取的 —— 这意味着 CLAUDE.md 一经修改，
某次运行当时的版本就无从恢复，于是前后对比只能显示成本下降，却说不出**因为改了什么**。
`harness_snapshots` 解决这一点：在启动时、每次经由本应用写入配置后，以及每 15 分钟，
各采集一份指纹。这个间隔就是变更时间线的分辨率；一次修改会被记到它之后的首次采集上。

只存储哈希与 token 数，因此该日志不会泄露应用本来不展示的任何内容。MCP 服务器**仅按
定义**记录 —— 探测会启动每个已配置的服务器，且其缓存是进程内的，基于探测的指纹既昂贵
又在跨进程时不稳定。MCP 的 token 成本本来就已由事件流实测得出。

快照与其他数据一同过期，但有一个例外：早于截止点的最新一行会被保留，因为它正是最早那段
保留期用来做差异对比的基线。

## 数据保留

缓存只保留一个滚动时间窗 —— `data/retention.json` 中的 `retentionDays`，默认 30 天，可
在 **Settings（设置）** 页面修改。清理在启动时执行，之后每小时一次：

1. 删除早于截止时间的 `turns` 与 `events`；
2. 再删除既没有任何 turn、最后活跃时间又早于截止时间的 `agents`；
3. 最后删除已经没有任何 agent 的 `runs`。

因此，开始于截止时间之前但仍在进行的会话只会被裁剪，而不会被整个丢掉；尚未记录任何
turn 的新智能体也不会被误判为过期数据。

`files` 表刻意不参与清理：它保存每个转录文件已解析到的字节偏移，清掉某一行会让下一次
扫描重读整个文件，把刚刚删掉的记录又插回来。这也正是**调大**窗口时会故意清空 `files`
并重新扫描的原因 —— 那是把仍留在转录文件里的历史找回来的唯一办法。

截止时间取 `retentionDays - 1` 天前的本地零点，因此窗口覆盖完整的自然日，与最宽的图表
范围严格对齐；同时它不会晚于 24 小时前，这样即使设成 1 天，`24h` 范围也仍然完整。

## 成本估算

这里的成本是 **API 等价参考值**，不是账单。每个回合的 token 按一张分模型价格表
（每百万 token 的 输入 / 输出 / 缓存写入 / 缓存读取 单价）计价，你可以在 **Settings**
页面或直接在 `data/pricing.json` 中编辑。缓存写入按输入价的 1.25 倍（5 分钟 TTL）或
2 倍（1 小时 TTL）计价；缓存读取按输入价的 0.1 倍计价。

价格表中没有列出的模型 id 会回退到所属系列的标准价，因此新发布的模型仍会被估算，而不会
被当成免费。

如果你用的是 Pro/Max 订阅，这些数字告诉你同样的工作走 API 会花多少钱 —— 它们不是你
实际被收取的金额。Anthropic 没有提供查询订阅席位用量的程序化 API；那部分请见
<https://claude.ai/settings/usage>。

## 套餐权重

订阅速率限制不是按美元计量的。每次调用的计分是

```
(cache_read + input×10 + cache_write×12.5 + output×50) × tier
tier：fable 10，opus 5，haiku 1，其余 3
```

这就是以一次缓存读取为单位的 API 价格比例，而 tier 即该系列每 1M 的输入价格。因此在
两张表一致的地方，套餐权重与 API 成本是同一个量，只差一个常数：**1 美元 =
10,000,000 权重单位**。

两者有两处不一致，都是有意为之：

- 1 小时缓存写入按输入价 2× 计费，但只按 1.25× 计权，所以 1h 缓存密集的工作花的钱
  多于它消耗的额度；
- tier 按系列划分，因此漏掉了价格表确实收录的按模型费率（旧版 Opus 4.x 每 1M 15 美元、
  Sonnet 5 首发价每 1M 2 美元）。

权重由费率卡计算，而非从成本推导，所以编辑 `pricing.json` 不会改变所报告的套餐计量口径。

## 上下文占用

`/context` 统计的是最近一次 API 调用的输入侧 —— `input + cache_creation +
cache_read` —— 并对照模型的窗口。每个运行都会从自己的回合算出同一个数字：
`context.lastTokens` 是最近一次调用，`context.peakTokens` 是被压缩重置的最高水位。
子智能体的回合被排除，因为它们各自运行在自己的窗口里。

窗口大小本身不记录在转录文件中，因此占用以 token 数报告，而非百分比。
